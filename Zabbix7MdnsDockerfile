# Use the Zabbix server image as the base
FROM zabbix/zabbix-server-pgsql:ubuntu-7.0-latest

# Switch to root user
USER root

# Commented out: Install additional packages using apt-get
#RUN apt-get update && apt-get install -y \
#    nano \
#    avahi-daemon \
#    avahi-utils \
#    dbus \
#    libnss3-dev\
#    bash

# Commented out: Copy a custom avahi-daemon.conf if you have specific configurations
#COPY avahi-daemon.conf /etc/avahi/avahi-daemon.conf

# Commented out: Setup D-Bus
#RUN dbus-uuidgen > /var/lib/dbus/machine-id

# Commented out: Modify nsswitch.conf
#RUN echo 'hosts: files mdns_minimal [NOTFOUND=return] dns' >> /etc/nsswitch.conf

# Commented out: Expose the mDNS UDP port 5353
#EXPOSE 5353

# Commented out: Copy new scripts or configuration files if needed
#COPY custom-script.sh /usr/local/bin/custom-script.sh

# Commented out: Make sure the custom script is executable
#RUN chmod +x /usr/local/bin/custom-script.sh

# Commented out: Switch back to the non-root user before setting the ENTRYPOINT
#USER 1997

# Use the default entry point and command from the base image to start Zabbix server
CMD ["/usr/sbin/zabbix_server", "--foreground", "-c", "/etc/zabbix/zabbix_server.conf"]
